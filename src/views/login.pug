extends base 

block content 
    if errorMessage 
        span=errorMessage
    form(method="POST")
        input(name="username", placeholder="이름", type="text", minLength=2, maxLength=10, required)
        input(name="password", placeholder="비밀번호", type="password", minLength=8, maxLength=16, required)
        input(type="submit", value="로그인")
        br 
        a(href="/users/kakao/start")
            img(src="//k.kakaocdn.net/14/dn/btroDszwNrM/I6efHub1SN5KCJqLm1Ovx1/o.jpg", width=222)

    
    small 아직 계정이 없으신가요?
        a(href="/join") 가입